import React from 'react';
import { View, Text } from 'react-native';

export default class RankScreen extends React.Component {
  render() {
    const { navigate } = this.props.navigation;

    return <Text>rank screen</Text>;
  }
}
